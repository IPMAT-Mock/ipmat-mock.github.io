{"enabled": false, "notes": "Copy to llm.config.json and fill in keys (that file is git-ignored — never commit keys). Pick provider per call from the admin LLM tab. OpenAI-compatible needs base_url/model/api_key; Gemini needs model/api_key only (get a key at https://aistudio.google.com/apikey).", "provider": "openai", "providers": {"gemini": {"api_key": "", "model": "gemini-3.5-flash-lite"}, "openai": {"api_key": "", "base_url": "https://api.openai.com/v1", "model": ""}}}
